class WeatherModel{
  double ? temp;
  String ? name;
  String ? image;
  String ? date;
  String ? status;
  WeatherModel(
  {
    this.image,
    this.name,
    this.date,
    this.temp,
    this.status,

}
      );

}